name: Deployment

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: deploy
        run: |
          echo "Hello from ${{ var.MY_ENV_NAME }}"
